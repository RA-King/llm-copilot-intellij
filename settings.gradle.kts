rootProject.name = "llm-copilot-intellij"

pluginManagement {
    repositories {
        maven("https://packages.jetbrains.team/maven/p/ij/intellij-plugin-service")
        gradlePluginPortal()
        mavenCentral()
    }
}
